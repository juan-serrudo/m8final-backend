apiVersion: v1
kind: Service
metadata:
  name: password-manager-api-loadbalancer
  namespace: default
  labels:
    app: password-manager-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: password-manager-api

