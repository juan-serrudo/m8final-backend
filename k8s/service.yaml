apiVersion: v1
kind: Service
metadata:
  name: password-manager-api-service
  namespace: default
  labels:
    app: password-manager-api
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: password-manager-api

